import * as React from 'react';
import SectionForces from './SectionForces';

const App: React.StatelessComponent = () => (
	<div className="App">
		<h1>Pressure</h1>

		<h2>Values</h2>
		<SectionForces />
	</div>
);

export default App;
